import 'package:flutter/material.dart';

class applied_page extends StatelessWidget {
  const applied_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue,
    );
  }
}
